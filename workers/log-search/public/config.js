// Same-origin app; kept for parity with the other tools. The log search runs
// entirely in the browser, so there is no backend API to point at.
window.logSearchConfig = { apiBaseUrl: window.location.origin };
