{
  "name": "rig-deployment-dashboard",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "migrate:local": "wrangler d1 migrations apply rig-deployment-dashboard --local",
    "migrate:remote": "wrangler d1 migrations apply rig-deployment-dashboard --remote"
  },
  "devDependencies": {
    "wrangler": "4.105.0"
  },
  "dependencies": {
    "jose": "^6.2.3"
  }
}
