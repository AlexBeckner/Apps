name = "dataspeed-parameter-proxy"
main = "src/index.js"
compatibility_date = "2024-09-23"

[vars]
# Same-origin Worker-hosted app. Override to "*" in local `.dev.vars` if needed.
ALLOWED_ORIGIN = "https://dataspeed-parameter-proxy.dataspeedhashfinder.workers.dev"
